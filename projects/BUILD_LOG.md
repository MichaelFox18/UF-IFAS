# Build log — modular R Shiny kit

Running notes on the AI-assisted "document then rebuild" workflow: what worked, what didn't, and prompts worth reusing. James asked for this; append a dated entry per experiment.

---

## 2026-06-16 — New feature: row Filter (in the Import tab)

Michael hit a real gap testing a large NBA-stats dataset: there was no value-based row filter (Reshape→Subset only does random sampling + column-keep). Chose to put filtering **inside the Import tab** so Import is the one "get your data ready" stage (load → Data Health → recast → filter). Built as a shared module feature, so all three apps gain it.

- **`R/helpers_filter.R` (pure base R).** `apply_filters(df, conditions)` AND's a list of conditions; each is a plain `list(col, op, value)`. Operators by type: numeric `between / >= / <= / > / < / == / !=`, date `between`, categorical `in / not_in / contains` (case-insensitive literal substring). NA rows drop (like dplyr::filter); unknown columns / unusable values are no-ops, never errors, so a half-built or stale condition can't break the pipeline. `describe_condition()` renders the chip text. `filter_mask()` is the per-condition helper. **Gotcha:** `grepl(fixed=TRUE)` ignores `ignore.case` (and warns) — did case-insensitive `contains` by lower-casing both sides.
- **`tests/testthat/test-filter.R`** — 18 expectations (numeric/between/in/not_in/contains, AND of conditions, NA drop, empty/unknown-column no-ops, describe_condition). **Suite 190 green** (was 176).
- **`mod_import` Filter panel.** New "Filter rows" card: a type-aware builder (column → operator adapts to type → value input(s)), an "Add filter" button, removable filter chips, "Clear all", and a live **"Keeping M of N rows"** counter. State lives in `rv$filters`; a bounded pool of 20 remove-observers handles per-chip deletion (the fixed-pool pattern avoids dynamic-observer double-firing). `filtered()` = `apply_filters(rv$data, rv$filters)` and is now what the module **returns** and what the preview/profile/summary/caption describe (Data Health + type-recast still operate on the full working copy). Filters reset on load/example/clear. Local `is_num_col`/`is_date_x` predicates keep the module from needing `helpers_plot` (reshape_tool/combine_tool don't attach it).
- **Wired** `helpers_filter.R` into all three app source blocks (it sits before `mod_import`). The two mini-apps get filtering for free.

PowerShell `testServer` smoke: load mtcars (32) → add `cyl > 5` → 21 rows → clear → 32; all three apps parse. **Next:** Michael to try it on the NBA dataset (e.g. team is any of …, minutes ≥ 20); then report export or the package conversion.

---

## 2026-06-16 — New feature: Compare Groups tab (hypothesis testing)

First net-new feature of the polish phase. Michael chose "Full + chi-square" scope. Built as an analysis stage (a tab, like Regression — *not* a standalone mini-app; that stays a cheap ~50-line wrapper for later if students want it). Followed the house path: pure helper + tests → module → dev harness → wire into the app.

- **`R/helpers_compare.R` (pure base/stats).** `compare_groups_numeric()` picks the test from #groups × parametric flag: Welch/Student t-test (+ Cohen's d) for 2 groups, one-way ANOVA + Tukey HSD (+ eta-squared) for 3+, or Wilcoxon / Kruskal–Wallis non-parametric. Assumptions: `normality_table()` (per-group Shapiro–Wilk, NA where n∉[3,5000] or constant) and `levene_test()` (Brown–Forsythe, median-centred, implemented in base R to avoid a `car` dependency). `compare_categorical()` runs chi-square + Cramér's V with a Monte-Carlo Fisher's-exact fallback when any expected cell < 5. `effect_magnitude()` buckets d/eta²/V into negligible/small/medium/large. All return structured lists/data frames or NULL.
- **`tests/testthat/test-compare.R`** — 30 expectations checking the helpers against the raw `t.test`/`aov`/`chisq.test` they wrap (p-values equal), the Tukey row count, non-parametric routing, NULL on bad input, Levene flagging unequal variance, and Cramér's V bounds. **Suite 176 green** (was 146).
- **`modules/mod_compare.R`** — two modes (numeric-by-group / two-categorical) behind a radio; auto-everything (no "which test?" prompt — it's derived). Result card, plain-English verdict (significant or not + effect-size magnitude), assumption-checks card (normality table + Levene, with steer-to-non-parametric warnings), Tukey post-hoc table (ANOVA only), contingency table (cat mode), a reused `build_full_plot` boxplot/bar, and a `.txt` results export. Reuses `numeric_cols`/`groupable_cols` and `build_full_plot`. Gotchas hit: can't embed a `renderDT` inside `renderUI` (needs a `DTOutput` placeholder + separate render); `layout_columns` with a variable card list needs `do.call`, not `!!!`.
- **`dev/run_compare.R`** — boots the module with iris (ANOVA) + mtcars (chi-square).
- **Wired into `data_explorer`**: sourced the helper+module, added a "Compare Groups" tab (flask-vial icon) between Visualize and Regression, added it to `fillable`, updated the About workflow list to seven steps.

PowerShell `testServer` smoke: iris ANOVA F-test p≈1.7e-31, eta²=0.62, 3 Tukey rows; Kruskal–Wallis path; mtcars cyl×gear χ²=18.04, p=0.0012, Cramér's V=0.53, Fisher fallback 5e-04; boxplot builds; app.R parses. **Next:** Michael to test the tab in the running app; then either more features (report export was the runner-up) or start the R-package conversion.

---

## 2026-06-16 — Polish phase, round 1 (Data Explorer fixes from testing)

Start of the "polish then package" phase: the boss wants this turned into a real R package, but we agreed to do thorough bug-fixing/tests/final touches first. This is the first batch, all from Michael's testing of `data_explorer` (mtcars).

- **About tab was stale/wrong.** It listed only 4 steps (Import→Reshape→Summarize→Export) and told users "Visualize and Regression are coming" — both have shipped. Rewrote both About cards in `apps/data_explorer/app.R` to cover all six tabs (incl. Data Health, proportions, charts+code, diagnostics) and refreshed the Tips (full-screen button, Data Health). Updated the file header comment too.
- **Legend position did nothing in the interactive view (only "Hidden" worked).** Root cause: `ggplotly()` ignores ggplot's top/bottom legend placement — it always draws on the right unless hidden. The static PNG/PDF export already respected it, so only the on-screen preview was wrong. Added `plotly_legend_layout()` (helpers_plot.R) mapping the position to a plotly `layout(legend=...)`, applied in `mod_visualize`'s `renderPlotly`. Bottom/top now reposition; right/none unchanged.
- **Grouped bar legend showed "(4,1)/(6,1)/(8,1)" instead of "4/6/8".** Classic `ggplotly()` dodged-trace naming quirk — the trailing `,1` is the panel index leaking into the legend. Added `clean_plotly_trace_names()` (regex-strips `(level,N)` → `level`, leaves clean names alone), applied in the same `renderPlotly`. Smoke-confirmed: raw `(4,1)|(6,1)|(8,1)` → `4|6|8`.
- **Faceted charts overlapped/cramped.** Can't fully fix in a 4-up grid, but reduced it: `build_full_plot` now adds `panel.spacing` + compact bold strip labels on a light band when faceting, and `mod_visualize` gives a faceted slot +170px height. Pointed users at the per-card full-screen button (cards already have `full_screen = TRUE`) in the About tips.
- **Export tab was missing summary + regression previews** (it already previewed data + charts). `exportServer` gained `summary_tbl = NULL`; when supplied it shows a Summary preview (DT) + a "Summary table (.csv)" download, and the model block now renders a Regression preview (model `summary()` printout + a one-line R²/significance headline via `model_interpretation`). Wired `summary_t <- summarizeServer(...)` through to `exportServer` in the app. Blocks are still gated on their reactive, so `reshape_tool`/`combine_tool` are unchanged.

New pure helpers got tests (`test-plot.R`: `plotly_legend_layout`, `clean_plotly_trace_names`, incl. the no-trace edge case). **Suite 146 green** (was 136). PowerShell smoke: faceted boxplot builds with the new theming; grouped-bar trace names clean up; all changed files parse; `exportServer` formals now include `summary_tbl`.

**Same-session follow-up — regression diagnostic plots in Export.** Michael flagged that the Export tab previewed/downloaded the regression *text* but not its graphs. Added them inside `mod_export`'s `model` block: the Regression preview card now renders the two diagnostic ggplots (`reg_fitted_gg` + `reg_resid_gg`) side by side via `draw_plot_grid`, and the Export-regression sidebar gained a PNG/PDF selector + a "Diagnostic plots" download wired through `render_plots_to_file` (6×4.5 each, 150 dpi). Reuses existing tested helpers, so no new unit tests; smoke-confirmed the plots build and a 24 KB PNG writes through the download path. Suite still 146 green.

**Next (polish phase):** test the reshape_tool and combine_tool apps; add committed `testServer` module tests before packaging; then start the R-package conversion.

---

## 2026-06-09 — Experiment 1: generate the JMP Tables menu spec

**Goal.** Have the AI produce an explanation file of JMP's Tables menu, detailed enough to rebuild in R, as both James's deliverable and the spec for the `reshape` module.

**What worked.**
- The R/tidyverse mappings came out reliable and idiomatic — `pivot_longer`/`pivot_wider`/`*_join`/`bind_rows`/`rows_update` etc. This is the AI's strong suit and the part that doesn't need checking.
- The per-operation template (what JMP does → dialog options → R equivalent → worked example → module notes) is reusable. It's the format we'd repeat for cloning any other menu, so it's worth keeping as a house style.
- Surfaced a design decision we hadn't named: the menu is really *two* modules, not one — single-table reshapes vs two-table combines (Join/Concatenate/Update). Good catch that came out of writing the spec rather than from planning.

**What didn't / watch-outs.**
- The AI's *unaided* knowledge of JMP's exact dialog option names and version-specific items was approximate. A quick check against JMP's own docs was needed to confirm specifics (Multiple Series Stack + Contiguous, Split By, Update-in-place / "replace columns" gotcha, Join by row number vs matching vs Cartesian). So: product-specific UI details need verification; concepts and R do not.
- Exact dialogs differ across JMP 16/17/18 — the spec flags this but it's unresolved until we confirm the team's version.

**Prompt notes.**
- The original broad prompt ("explain everything in the Tables menu, detailed enough to recreate in R") produces *breadth* — a once-over of every item. Good for a first map, thin on depth.
- Better pattern for depth: ask per operation, and explicitly request the R equivalent + a worked before/after example each time. That's what made Stack/Split usable.
- General rule emerging: let the AI do the conceptual translation and R, but verify the source product's specifics against its real docs/install before trusting them.

**Recommended process for future clones.**
1. Pick a bounded menu/feature (not a whole product).
2. Generate the op → options → R → example table.
3. Verify the product-specific bits against the actual app/docs.
4. Build one Shiny module per coherent group of operations.

**Open items / decisions.**
- Confirm JMP version for the verify pass (16 / 17 / 18?).
- Lock module wiring: returns-and-arguments (Pattern A) vs shared `reactiveValues` store. Leaning Pattern A, shared store only for true global state.
- Loose folders now, convert to an R package once the shared library stabilises.

---

## 2026-06-09 — Experiment 2: build the `reshape` module (task 1)

**Goal.** Stand up the modular kit's first brick: the single-table `reshape` module with Stack + Split, as pure helpers → tests → module → dev harness, per the CLAUDE.md definition of done.

**Decisions locked (with James).** Modular rebuild from scratch in `projects/` (don't carve up the monolith); Stack + Split first; must run both locally and deploy cleanly; full rigor (testthat + this log). These match the existing plan, so no doc changes were needed.

**What got built.**
- `R/helpers_reshape.R` — `do_stack()` (verbatim from the spec) + `do_split()` wrapping `pivot_wider` (roles: `value_col`/`split_by`/`group_cols`, `group_cols = NULL` ⇒ "everything else", optional `values_fn` for duplicates).
- `tests/testthat/test-reshape.R` — 8 tests incl. happy path + an error case for each verb, custom names, `values_fn` de-dup, and a stack↔split round-trip. `tests/testthat/setup.R` auto-sources `R/` so future test files don't repeat it. **All pass.**
- `modules/mod_reshape.R` — `reshapeUI()` / `reshapeServer(id, data_in)`, Pattern A (returns the reshaped reactive). Operation selector with conditional Stack/Split controls; column pickers sync to incoming data; `validate(need())` guards.
- `dev/run_reshape.R` — boots the module alone with two built-in samples: `relig_income` (wide → Stack) and `fish_encounters` (long → Split), themed UF blue/orange.

**What worked.**
- The pure-helper-first discipline paid off: `do_split` was a 10-line wrapper and the round-trip test caught nothing because the contract was already right. Helpers stayed Shiny-free, so they test in milliseconds with no app boot.
- `testServer()` exercises the module's reactive logic headlessly — confirmed Stack (4 rows) and Split (2×3) without launching a browser. Good fit for "deployment-safe + tested."

**What didn't / watch-outs.**
- **Rscript segfaults (exit 139) when loading `shiny`/`bslib` under the git-bash shell** on this machine; the same script runs clean under native PowerShell (`Rscript.exe`). Pure-helper/testthat runs are fine under bash (they don't load Shiny). **Rule: run anything that loads Shiny via PowerShell, not bash.**
- First smoke test was wrong, not the code: I tried to Split the *output* of a Stack, but the module reshapes its `data_in`, not a chained result. Confirms the design — chaining stack→split is the parent app's job (re-feed the reactive), and the module's contract stays "one reshape of the input."
- **`here::here()` would resolve to the outer `UF-IFAS/.git` root**, not the kit. Fixed by dropping a `.here` anchor in `projects/` so it resolves to the kit root from any cwd.
- **UF blue mismatch:** CLAUDE.md specifies `#0021A5`; the existing monolith hardcodes `#003087`. Used `#0021A5` per the spec. *Flag for James to confirm the canonical brand blue before R/components.R is extracted.*

**Prompt notes / process.** Asked James the four direction-defining questions up front (build strategy, first-feature scope, run target, rigor) before writing code — cheaper than guessing and redoing. The CLAUDE.md "definition of done" was specific enough that task 1 needed no further clarification.

**Run it.**
- Tests: from `projects/`, `Rscript -e 'testthat::test_dir(here::here("tests/testthat"))'`
- Module alone: `shiny::runApp("dev/run_reshape.R")` (launch via PowerShell)

**Next.** Roadmap step 2 — extract `R/components.R` (one UF theme + `info_tip`/value cards/download config) from the monolith, resolving the blue first. Then more single-table verbs (transpose/sort/subset/summary) behind the same operation selector.

**Same-session follow-up — themed the harness (roadmap step 2, started).** James flagged the harness looked too plain and wanted the original Data Explorer's UF look. Found that look is just `bs_theme(bootswatch = "flatly", primary = UF_BLUE, "navbar-bg" = UF_BLUE)` + the inlined white IFAS logo in a `page_navbar` title — no custom CSS. Extracted that into `R/components.R`: `UF_BLUE`/`UF_ORANGE`/`UF_COLORS`, `uf_theme()`, `uf_logo_uri()` (base64-inlines `www/IFAS-White.png`, found via `here::here()`), `uf_title()`, and `info_tip()`. Copied the logo to `projects/www/`. Rebuilt `dev/run_reshape.R` as a `page_navbar` using these, with the sample picker in the navbar. **Blue resolved:** matched the original `#003087` (not the plan's `#0021A5`) since James asked for the existing look — it's a one-line change in `components.R` if the brand guide says otherwise. Smoke test (PowerShell): logo resolves, theme + navbar page build; unit tests still green. Watch-out: `uf_logo_uri()` relies on `here()` anchoring to `projects/`, so a session whose `here` root is the outer repo won't find the logo — launch the app from inside `projects/`.

## 2026-06-10 — Bug fixes (export download, boxplot cut-off, chart preview)

From James's testing:
- **Export chart download was broken** (browser saved `ex-dl_plots.htm` + "something went wrong"). Root cause: `mod_visualize` *referenced* `current_plots()` in its returned reactive but the **function was never defined** in the port — so `plots()` threw "could not find function", the downloadHandler errored, and the browser got Shiny's HTML error page. It only surfaced on Export because the on-screen plots use separate render outputs. Defined `current_plots()` in the module (builds the 1–4 configured plots, `tryCatch` → NULL per slot). Verified: returns 4 ggplots; PNG (49 KB) + PDF render.
- **No chart preview on Export.** Added a "Chart preview" card (shown only when a `plots` reactive is supplied) that renders the exact `draw_plot_grid()` grid the download produces.
- **Boxplot "cut-off" oddity** (found via warnings while reproducing the export). A *numeric* boxplot X (e.g. `cyl`) was left continuous, so ggplot drew one mis-sized box ("did you forget `aes(group=...)`?") — which is why grouping "fixed" it. Now coerce a numeric boxplot X to a factor (one box per value); mirrored in `generate_code`. Warnings gone.

Suite **135 green**; PowerShell smoke reproduced the 4-plot Export scenario end to end.

## 2026-06-10 — Supervisor changes, round 2 (line/bar + proportions)

Finished the supervisor's list. James chose **Both** for the line/bar item and "my recommendation" for proportions (a new mode in the Summarize tab).

- **Line now connects summary stats.** `build_full_plot()`'s line branch aggregates Y by X (within each group) with a chosen function (`line_agg`, default mean) and connects those — for a continuous X with one row each this is the identity (unchanged); for repeated/categorical X it connects the per-category mean/median/sum instead of scribbling raw rows. The smooth overlay was parameterised (`smooth_layer(ycol)`) so it fits on the aggregated `.value`.
- **Bar gains a connecting-line overlay.** New `bar_line` toggle adds `geom_line`+`geom_point` over the (aggregated) bar tops; grouped bars + line share `position_dodge(width = 0.9)` so the line sits on its bars. `generate_code()` mirrors both (line → `dplyr::summarise` + `.value`; bar → appended overlay). New UI: a "Connect (per X)" picker for line and an "Overlay a connecting line" checkbox for bar; `slot_params` carries `line_agg`/`bar_line`. Verified via `ggplot_build`: line→3 vertices over gear, continuous line stays 1:1, bar shows GeomCol+GeomLine+GeomPoint.
- **Proportions-by-group summary** (`binom` installed 2026-06-10). New pure `proportions_summary(df, outcome, groups, conf_level)` → per group, percent of each level of a categorical outcome with **exact Clopper–Pearson CIs** via `binom::binom.confint(..., methods = "exact")`; returns `[groups…, Level, N, Total, Percent, CI_low, CI_high]` (0–100). `mod_summarize` now has a **Means / Proportions** radio: Means = the existing numeric summary; Proportions = pick a categorical outcome + grouping + confidence level. Shared groups picker; mode-aware caption, card title, and download stem (`group_proportions_DATE.csv`). `binom` is namespace-qualified so apps need it installed but not attached.

Tests: code-gen checks for line-agg + bar-overlay in `test-plot.R`; `proportions_summary` + SE/IQR in `test-stats.R` — suite now **134 green**. PowerShell smoke: data_explorer assembles; Visualize passes line_agg/bar_line through; Summarize Means (3 groups, SE) and Proportions (6 rows, exact CIs bracket the %) both work. **The supervisor's full list is now done.**

## 2026-06-10 — Supervisor changes, round 1 (graph fixes + summary SE/IQR)

Started the supervisor's list (see memory `supervisor-graph-summary-changes`). Used a `ggplot_build()` diagnostic to ground each fix empirically before changing code.

- **Regression overlay now fits per group** (confirmed bug: diagnostic showed `smooth groups = 1` when a colour var was set). `smooth_layer()` now maps `color = group` into `geom_smooth` when a colour var is present (one fit per group, coloured to match points); single fit in `reg_col` otherwise. Mirrored in `generate_code()` (`geom_smooth(aes(color = g), …)`). Verified: 2 groups drawn.
- **SQRT axis scale added** alongside log. The `logscale` control is now "Axis scale" with `none / logx / logy / logboth / sqrtx / sqrty / sqrtboth`; `build_full_plot()` and `generate_code()` parse `<log|sqrt><x|y|both>` and emit `scale_*_sqrt()` / `scale_*_log10()`. Verified the sqrt transform applies.
- **Boxplot grouping hardened.** Empirically, *categorical* group dodging already worked (2 groups × 3 gears → 4 dodged boxes); the real gap was a *continuous numeric* group (>10 distinct) which can't define boxes — now dropped via the same guard bar/histogram use, and grouped boxplots use explicit `position_dodge2(preserve = "single")`. (If the supervisor saw a specific categorical case look wrong, need a repro — categorical dodging tests clean here.)
- **Summary: added SE (= SD/√N) and IQR** to `grouped_summary()` (+ `.s_se`/`.s_iqr` reducers, column order, docstring, `mod_summarize` caption/help). SE is NA for n = 1.

Tests: added sqrt + grouped-smooth code-gen checks to `test-plot.R` and SE assertions to `test-stats.R` — suite now ~**120 green**. PowerShell smoke: data_explorer assembles; Visualize passes `sqrty` + grouped regression overlay through to a built plot + generated code. **Still to do:** proportions-by-group summary with exact CIs (`binom`), and the line-as-bar-overlay (needs a behaviour decision).

**Same-session follow-up — Export enrichment, fillable polish, docs, full verification.**
- **Export (1):** ported the monolith's remaining Export features. Added `draw_plot_grid()` + `render_plots_to_file()` (base grid, png/pdf/svg) to `helpers_plot.R`; `mod_visualize` now **returns `reactive(current_plots())`** (the live ggplot list, lazy); `mod_regression` already returned the model. `exportServer(id, data_in, plots = NULL, model = NULL)` gained an optional **Export charts** block (format/size/DPI → image grid) and **Export regression** block (summary .txt, coefficients/fitted/residuals .csv), each rendered only when its reactive is supplied — so reshape_tool/combine_tool (no plots/model) are unchanged. `data_explorer` now captures `plots <- visualizeServer(...)` and `model <- regressionServer(...)` and passes both to Export.
- **Fillable (2):** gave every `data_explorer` and `reshape_tool` nav_panel an explicit `value` and switched `fillable` to those values — previously `fillable` held plain strings that never matched the tagList (icon+text) titles, so those tabs scrolled instead of filling.
- **Docs (3):** rewrote `CLAUDE.md` (Current state, real repo layout, env gotchas, roadmap marked ✅) and wrote a user-facing `README.md` (prerequisites, how to launch each app, what each does, how they share modules).
- **Verification:** unit suite **115 green**; PowerShell end-to-end smoke confirmed all 3 apps + both dev harnesses assemble, the data_explorer pipeline (stack 3×4→9×3 → summarize-by-group), a combine full-join, and Export rendering a 2-up PNG + a PDF.

**Same-session follow-up — `combine_tool` mini-app (combine's real home).** Gave the combine module a runnable app: two full `mod_import` instances (Left + Right — each with upload/clean/recast) → `mod_combine` → `mod_export`, a `page_navbar` under the UF theme. Small enabling change: `importUI()` now takes `example_choices` (defaulting to the old three) so the tool can offer join-friendly examples (`band_members`, `band_instruments`, two mtcars halves) wired to a matching `examples` list passed to each `importServer`. Gave the nav panels explicit `value`s so `fillable = "combine"` targets only the Combine tab (the import tabs scroll — their stacked health/profile cards shouldn't be squeezed). PowerShell smoke: app assembles, `importUI` takes custom choices, a custom example imports. Suite still **115 green** (the importUI default arg is backward-compatible — data_explorer/reshape_tool call it unchanged). **Run:** `shiny::runApp("apps/combine_tool")`. (Minor known cosmetic: data_explorer's `fillable` uses plain strings against tagList titles, so those tabs may scroll rather than fill — harmless; fix by giving those nav_panels explicit values too.)

**Same-session follow-up — the `combine` module (roadmap step 3, the two-table half).** Built the second module the JMP Tables menu implies — the operations that need a SECOND table, kept deliberately out of `reshape`. `R/helpers_combine.R` (pure, dplyr-qualified): `do_concatenate` (bind_rows, optional source tag, NA-fills non-shared cols), `do_join` (left/inner/full/right/cross with key validation), `do_update` (rows_update overwrite vs rows_patch fill, `unmatched = "ignore"`, updates restricted to shared columns so it never errors on extra cols), and `compare_tables` (one row per column: in-left/in-right + per-shared-column cell-diff count when row counts match). `modules/mod_combine.R` follows **Pattern A with two inputs** — `combineServer(id, left, right)` takes two data reactives, syncs the key pickers to the shared columns, and returns the NULL-safe result; Result/Left/Right preview tabs with a before+after shape line. `dev/run_combine.R` boots it with `dplyr::band_members` + `band_instruments` (the classic join-by-name example) and two mtcars halves (concatenate). `test-combine.R` covers all four ops + the missing-key errors — **115 expectations green**. PowerShell smoke: join(inner, name)=2 rows, concatenate=6 rows with source tag, compare=3 columns summarised. **Run:** `shiny::runApp("dev/run_combine.R")`. Note: integrating combine into an app needs a two-table source — the natural home is a `combine_tool` mini-app (two importers → combine → export), mirroring `reshape_tool`; deferred until James wants it.

**Same-session follow-up — full Data Explorer, slice 4 (enriched Import).** Brought the modular Import up to monolith parity. Extracted `R/helpers_clean.R` (the `clean_specs()` detect/fix engine — column names, whitespace, NA tokens, numbers-as-text, dates, empty cols/rows, dup rows — plus `detect_issues`/`clean_apply` and `convert_column`/`CONVERT_TYPES`; all base R) and added the profiling helpers (`friendly_type`, `column_profile`, `data_glance`) to `helpers_stats.R`. `mod_import` now keeps a **working copy + an immutable `data_raw`**: a Data Health panel lists detected issues as opt-in checkboxes (Apply mutates the working copy, Revert restores the upload), a Change-Variable-Types tool recasts a column (factor/number/integer/logical/date — factor→number routes via labels, not codes; reports values lost to NA), and the main area gains an at-a-glance summary, an advanced `summary()` accordion, and a per-column profile table. Both apps now source `helpers_clean` (+ reshape_tool also `helpers_stats`). Tests: `test-clean.R` (detect/apply/convert) + profiling tests in `test-stats.R` — **99 expectations green**. PowerShell smoke: both apps assemble; cyl→factor conversion, Health fixes (numbers-as-text + drop-empty + de-dup), and revert all verified. **Roadmap step 4 (port the monolith into modules) is complete — the modular Data Explorer now matches the original, feature for feature, across all tabs.** Only the separate `combine` module (join/concatenate/update) remains.

**Same-session follow-up — full Data Explorer, slice 3 (Regression).** Extracted `R/helpers_model.R`: `fit_model()` (linear / multiple / polynomial → builds the formula and `lm`s it), `model_interpretation()` (pulls R²/adj-R²/overall F-p/significant+non-significant predictors out of `summary()`), and the two diagnostic ggplots (`reg_fitted_gg`, `reg_resid_gg`) with `thin_rows`/`thin_note` for big-data responsiveness. `fit_model`/`model_interpretation` are base/stats only so they test under bash; `test-model.R` covers linear/poly degree/error/interpretation — **77 expectations green**. Built `mod_regression.R` (response + predictor pickers that adapt to model type, fit button, summary printout, colored plain-English interpretation, two plotly diagnostics, .txt export; resets the model when upstream data changes). **Parse gotcha:** a braceless multi-line `if/else` function body (`thin_note`) fails to source at top level with "unexpected 'else'" — wrap such bodies in `{}`. PowerShell smoke: app assembles; fitting mpg ~ wt + hp gives R² 0.83 with wt+hp significant; both diagnostics build. Wired into `data_explorer` (sourced helpers_model + mod_regression; `fillable` += Regression). **The Data Explorer now has the full analysis pipeline: About → Import → Reshape → Summarize → Visualize → Regression → Export.** Remaining: enrich Import (Data Health + type conversion + column profile), then the combine module.

**Same-session follow-up — full Data Explorer, slice 2 (Visualize, full parity).** James chose full parity. Extracted `R/helpers_plot.R` ~verbatim from the monolith (constants, `build_full_plot` for all 7 chart types, `build_corr_heatmap`, chart-suitability `chart_hint`, palette scales, and the `generate_code` ggplot2 emitter) — kept ggplot2 calls unqualified (the apps attach ggplot2) but qualified the few `dplyr::` calls so it's namespace-safe. Built `modules/mod_visualize.R`: 1–4 plot grid, per-plot config accordions, "apply Plot 1 style to all", reset, smart hints, auto static-vs-interactive (`BIG_ROWS`), and a copy-the-R-code panel per plot. **Namespacing was the work:** every input id and every `conditionalPanel` condition is built with `ns()`, and JS conditions use **bracket notation** (`input['viz-mp1_type']`) because namespaced ids contain a hyphen (dot notation breaks). `plot_slot_panel(ns, i)` takes ns and namespaces all ids + conditions; dynamic pickers created in `renderUI` also wrap ids in `ns()`. **Refactor:** moved `label_or()` from `mod_reshape.R` into `R/components.R` (shared util) — `helpers_plot` needs it and `setup.R` only sources `R/`, so tests were failing to find it. `test-plot.R` covers the ggplot2-free logic (chart_hint, generate_code parseability + backticking, lump_bar_x, bq/qq) — **69 expectations green**; PowerShell smoke builds all 7 chart types from mtcars + drives the module's scatter (plot + code). Wired into `data_explorer` (libraries: ggplot2/plotly/colourpicker; `fillable = c("Reshape","Visualize")`). App now: About → Import → Reshape → Summarize → **Visualize** → Export. Next: `mod_regression`.

**Same-session follow-up — full Data Explorer, slice 1 (skeleton + Summarize).** Started roadmap step 4 (port the monolith into modules). Build order agreed with James: skeleton → Summarize → Visualize → Regression → enrich Import → then the combine module; keep a runnable app at every step. This slice: extracted `R/helpers_stats.R` (`grouped_summary` + the NA-safe reducers/mode, plus `numeric_cols()` and `groupable_cols(max_levels = 30)` classifiers that feed the variable pickers), with `test-stats.R` (grouped stats, NULL-on-bad-input, classifiers, mode). Built `mod_summarize` (numeric vars × grouping vars → count/mean/median/mode/min/max/SD, CSV download, friendly validate states). Assembled `apps/data_explorer/app.R`: a `page_navbar` (UF theme) wiring **About → Import → Reshape → Summarize → Export**, Pattern A, with the analysis/export stages reading the post-reshape **working** data (Reshape defaults to pass-through, so by default they see the imported data unchanged). Suite now **54 expectations green**; PowerShell smoke: app assembles, Summarize gives 3 cyl groups with mean mpg@4cyl = 26.7. **Run:** `shiny::runApp("apps/data_explorer")`. Next: `mod_visualize` (the big one — chart-suitability guards, plot builder, code export).

**Same-session follow-up — reshape/import UX polish.** James picked all of: clear-data button, Original-vs-Result preview tabs, friendly empty states, inline help, before→after shape. Built: (1) **Clear-data button** in `mod_import` — resets the reactiveValues and, since shinyjs isn't installed, empties the `fileInput` by rendering it through a `uiOutput` keyed on a `file_token` that Clear bumps. (2) **Tabbed preview** — swapped the result card for `navset_card_tab` with Result + Original tabs so students see the transformation; the tab strip title is a **before→after shape line** (`Reshaped: 18 × 11 → 180 × 3`, or `Pass-through`/`finish choosing` states). (3) **Friendly empty states** via `validate(need())` instead of `req()`, so the preview reads “Import data on the Import tab…”. (4) **info_tip help** on the non-obvious controls (duplicate-handling fn, stratify-by, transpose header, and the Operation label). **Key fix:** split the reshape reactive into a user-facing `result()` (validates → nice messages) and a NULL-safe `returned <- reactive(tryCatch(result(), …NULL))` that goes downstream, so Export degrades gracefully instead of erroring when the reshape is empty/half-specified. **testServer gotcha:** `session$returnValue()` is NOT a method in this Shiny — to read a module's return in a test, name the returned reactive and call it (`returned()`), or read internal `reactiveValues` by name. Smoke test (PowerShell) green across empty/valid/partial states + clear-data; 42 unit tests still pass.

**Same-session follow-up — shipped the `reshape_tool` mini-app (roadmap delivered ahead of step 4).** Turned the dev harness into a real import → reshape → export app. Extracted the monolith's file readers into `R/helpers_io.R` (`detect_table_bounds`, `read_delim_smart`, `read_file_data`) — pure, namespace-qualified (`readxl::`, `utils::`) so the app needn't attach readxl; tests in `test-io.R` cover bounds detection + a CSV round-trip that auto-trims title/footnote lines (used `withr::local_tempfile`, which ships with testthat). Built `mod_import` (upload CSV/Excel/TSV/RDS or load a built-in example, text-file parse options, multi-sheet Excel picker; returns `reactive(data | NULL)`) and `mod_export` (CSV/Excel/RDS download, sanitised filename stem; terminal stage). Added a **"None (pass-through)"** op to `mod_reshape` (now the default) so data can flow Import→Export untouched until the user opts into a reshape. Wired all three in `apps/reshape_tool/app.R` with Pattern A (each stage's returned reactive feeds the next). Suite now **42 expectations green**; PowerShell smoke test: app assembles, import loads the mtcars example, export caption + filename sanitising both correct. **Run:** `shiny::runApp("apps/reshape_tool")` from `projects/`. Watch-out (re-confirmed): in `testServer`, `session$returnValue()` already *evaluates* the returned reactive — don't call it again; or just read the module's internal reactiveValues by name inside the test expr.

**Same-session follow-up — finished the single-table reshape verbs.** Added `do_sort()`, `do_subset()`, and `do_transpose()` to `helpers_reshape.R`, same house style (validate → delegate). Notes: `do_sort` uses base `order(..., method = "radix")` because radix is the only method that takes a per-key `decreasing` vector, and `unname()`s the keys so a column named "method"/"decreasing" can't collide with `order()`'s args. `do_subset` does optional column-keep + `slice_sample` (n or prop), stratified via `group_by(across())`, with a seed for reproducibility. `do_transpose` is a `pivot_longer |> pivot_wider` round-trip; coerces to character **only** when the transposed columns are mixed type, so an all-numeric transpose stays numeric. All three got happy-path + error-case tests — suite now 33 expectations, all green. Wired all five ops into `mod_reshape` behind the operation selector (conditional control panels; the Sort "descending for" checkboxes track the chosen keys; Subset shows N/% + stratify + seed only when sampling). Smoke test via PowerShell drove all five through `testServer` — stack/split/transpose/sort/subset all OK. **Reshape single-table set is now feature-complete** (Summary deferred — it overlaps the planned `mod_summarize`, so decide whether it lives there or here before building it).
